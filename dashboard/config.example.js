// Copy this file to config.js and set your deployed Worker URL (no trailing slash).
//   cp config.example.js config.js
// Local dev default is wrangler's dev server.
//   https://api.example.com   (or https://expense-tracker.<subdomain>.workers.dev)
window.API_BASE = "http://localhost:8787";
