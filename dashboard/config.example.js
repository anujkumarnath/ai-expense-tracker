// Copy this file to config.js and fill in your values.
//   cp config.example.js config.js
// config.js is gitignored — your real values stay out of the repo.

// Deployed Worker URL (no trailing slash), e.g. https://api.example.com
window.API_BASE = "http://localhost:8787";

// Google OAuth Web client ID (…apps.googleusercontent.com) for Sign in with Google.
window.GOOGLE_CLIENT_ID = "";
